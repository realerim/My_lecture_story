# 의존성은 pyproject.toml의 [project.dependencies]에 정의되어 있습니다.
# 아래처럼 패키지 자체를 설치하면 됩니다 (동기화가 어긋나지 않도록 이 파일은
# 실제 버전 목록을 중복해서 들고 있지 않습니다).
#
#   pip install -e .          # 실행에 필요한 것만
#   pip install -e ".[dev]"   # + pytest (테스트용)
-e .[dev]
